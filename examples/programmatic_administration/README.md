# Programmatic administration example

This example shows how to administer an llfix instance programmatically through the TCP administration interface.

The provided Python script, 'reset_sequence_numbers.py', connects to the administration endpoint and demonstrates how to reset the incoming and outgoing sequence numbers for a specific instance and session.
